# 前端 P0 结构性优化开发文档

> 创建日期：2026-09-20
> 执行分支：`front0920`（从 `front0919` 的 HEAD `96db735` 拉出）
> 状态：**第 1 步 ✅ / 第 2 步 App 层 ✅ / 组件收敛 ✅ / 第 3 步（Zustand）未开始**
> 最新提交：`5266777` + styles.css 修复；应用已在本地运行验证（http://127.0.0.1:8000）

---

## 0. 如果中途出事了，先读这一节

### 0.1 当前工作区状态（最后更新：第 1~2 步完成后）

已全部提交，工作区干净。关键提交：

| 提交 | 内容 |
| --- | --- |
| `57a0804` | wip：依赖安装 + router/main/navigation 初稿 |
| `beda838` | 第 1 步：React Router 接管路由 + 13 个页面懒加载 |
| `fb8427a` | 第 2 步 App 层：bootstrap/threads 迁入 TanStack Query |
| `5266777` | 组件收敛：审批卡片注册表化 + 激活预览去重 |
| （最新） | styles.css `@import` 顺序修复（见 §4.1 踩坑 2） |

关键文件：
- `frontend/src/router.tsx`：catch-all 路由 + `router` 模块单例
- `frontend/src/queries.ts`：query keys + `useBootstrapQuery` / `useThreadsQuery`
- `frontend/src/components/approval/toolDetails.tsx`：按 `tool_name` 注册的审批渲染器
- `frontend/src/components/approval/ActivationPreview.tsx`：共享激活预览
- `frontend/src/__tests__/renderApp.tsx`：App 测试的 QueryClient helper

### 0.2 一键回滚

```powershell
Set-Location D:\RAG\better
git checkout front0919                 # 放弃本分支改动，回到 front0919
git branch -D front0920                # 如需删除本分支
git stash list                         # 查看开工前 stash 的 WIP
git stash pop stash@{0}                # 恢复 WIP（chat-goal-tools 等）
```

WIP stash 说明：开工前 `front0919` 上有 20+ 个未提交文件（chat_tools、goal_tools、PlanPage 改动等），已 stash 为 `stash@{0}: On front0919: wip: chat-goal-tools + plan page changes (before front0920 frontend refactor)`。

### 0.3 依赖与环境

- Node `v24.14.0` / npm `11.9.0`（`D:\develop\nodejs`）
- 已知警告：`jsdom@30.0.1` 要求 Node `^22.22.2 || ^24.15.0 || >=26.0.0`，当前 `24.14.0` 会打印 `EBADENGINE` 警告。**这是警告不是错误**；PowerShell 会把 npm 的 stderr 输出误判为失败（exit code 1），判断 npm 是否成功要看输出里的 `added N packages`，或改用 `cmd /c "npm ... > log.txt 2>&1"`。
- 建议后续把 Node 升级到 `>=24.15.0` 消除警告。

### 0.4 本地运行与查看

```powershell
Set-Location D:\RAG\better
$env:DATABASE_URL = "postgresql://better_agent:change-this-local-password@127.0.0.1:5432/better_agent"
python scripts\start.py     # 先 npm run build 再起 uvicorn，serve http://127.0.0.1:8000
```

- PostgreSQL 用 compose 里的容器（`docker compose up -d postgres`）；`compose.yaml` 只有 postgres 服务，应用本身在宿主机跑
- `styles.css` / `App.tsx` 等静态资源由 uvicorn 从 `frontend/dist` 按 request 读取——**改前端后只需重新 `npm run build`，不必重启后端**（index.html 的 hash 引用会自动指向新产物）
- 模型未配 key 时应用照常启动，仅发消息返回结构化 503（`/api/model-readiness` 可查）

---

## 1. 背景与目标

### 1.1 现状诊断（改动前）

- `App.tsx` 手写路由：`readRoute` 解析 URL、`navigateTo` 手写 pushState + 手动派发 `popstate`、顶层 `followLink` 拦截锚点点击
- 14 个页面全部静态导入，**无代码分割**，首屏加载全部页面代码
- `api.ts` 5 万字符、45 个类型导入、所有 API 平铺，无缓存/去重/失效重取
- 流式状态（SSE 消息合并、offset 校验、事件恢复）在 `useThreadTelemetry.ts` 里用 React state 手写状态机
- 无路由库、无服务端状态缓存、无客户端状态库

### 1.2 P0 目标（本次范围）

| # | 事项 | 收益 |
| --- | --- | --- |
| 1 | React Router 接管路由 + 页面级 `React.lazy` 代码分割 | 首屏体积下降；为后续按路由粒度演进打底 |
| 2 | TanStack Query 接管服务端状态（缓存、去重、失效） | 消灭重复的 `useEffect + fetch` 样板 |
| 3 | Zustand 收敛 `useThreadTelemetry` 的流式状态 | 流式逻辑从组件剥离，可单测、可复用 |

**明确不做**（本次范围外）：拆巨兽页面（PlanPage/TodayPage/ChatPage）、SSR、视觉改版、轨迹回放增强。

### 1.3 非目标与约束

- 不改动 `frontend/src/api.ts` 的函数签名（Query 只在调用侧包一层）
- 不改变任何 URL 结构和页面行为，所有现有链接必须继续可用（`design.md` 要求：Keep legacy links functional）
- 50 个前端测试文件 + Playwright e2e 必须全绿才算每一步完成
- 每一步独立提交，可单独回滚


---

## 2. 关键设计决策

### 2.1 路由迁移策略：适配层先行，不大改调用方

`navigation.ts` 的 API（`navigateTo`/`readRoute`/`pagePaths`/`todayPath`）被 5 个源码文件 + 2 个测试文件使用。决策：**保留 API 表面，内部改为驱动 React Router**，调用方零改动或极少改动。

- `router.tsx` 使用 `createBrowserRouter`，路由为 catch-all `*` → `<App />`（**不做** per-route children：现有页面依赖 App 注入的 `csrfToken`/`run`/`threadId` 等 props，拆成独立 route element 会迫使 14 个页面全部改写 props 获取方式，风险失控）
- `navigateTo` 内部改为 `router.navigate(path, { replace })`；`router` 是模块级单例，`navigation.ts` 直接 import 即可在非组件环境调用
- `App.tsx` 原来监听 `window popstate` 改为订阅 `router.subscribe(...)`（保留 popstate 监听无害）；路由解析仍用 `readRoute`
- 测试不受影响：测试直接渲染 `<App />` 时不需要 Router Provider，因为 App 不消费 router context，只通过单例 `router` 对象订阅事件

### 2.2 代码分割策略

14 个页面在 `App.tsx` 中改为 `React.lazy` 动态导入，外层包 `<Suspense>`，fallback 为统一加载态（样式类 `workspace-page-loading`）。`ChatPage` 是首页（`/`），如构建分析显示它占首屏大头，可改回静态导入（首屏本来就要它）。

### 2.3 TanStack Query 策略（第 2 步）

- `QueryClientProvider` 已在 `main.tsx` 就位（`staleTime: 30s, retry: 1`）
- 替换顺序：先替换 App 顶层的 `getBootstrap` / `listThreads`，再逐页替换；每替换一处跑对应测试
- SSE 推送的数据**不**进 Query 缓存（实时流用 Zustand，见 §2.4）；Query 只管"拉取式"服务端状态

### 2.4 Zustand 策略（第 3 步）

- 新建 `frontend/src/stores/threadStreamStore.ts`，把 `useThreadTelemetry` 里的消息列表、pendingAsk、流式 offset 校验、事件恢复逻辑迁入
- `useThreadTelemetry` 变为薄封装：订阅 store + 暴露与原 hook 相同的返回接口，保证 `ChatPage` 等调用方不改
- 迁移完成标志：`useThreadTelemetry` 内不再出现 `useState`


---

## 3. 验证方式（每一步必须全绿）

```powershell
Set-Location D:\RAG\better\frontend
cmd /c "npm test -- --run > test.log 2>&1"     # 单元/组件测试（约 50 个文件）
cmd /c "npm run build > build.log 2>&1"        # 类型检查 + 构建（输出各 chunk 体积，用于对比）
cmd /c "npm run e2e > e2e.log 2>&1"            # e2e（需要后端运行，可选但推荐第 1 步后跑一次）
```

基线对比：第 1 步完成后，对比 `build.log` 中 `dist/assets/index-*.js` 与懒加载产生的各页面 chunk 体积。

后端测试不预期受影响（本次不动 backend），如担心可跑：`Set-Location D:\RAG\better\backend; python -m pytest -q`

---

## 4. 实施步骤与进度

### 4.1 第 1 步：React Router + 代码分割 —— ✅ 已完成（commit `beda838`）

- [x] 安装依赖 `react-router-dom @tanstack/react-query zustand`
- [x] `router.tsx` 为 catch-all：`createBrowserRouter([{ path: "*", element: <App /> }])`，导出 `router` 单例
- [x] `navigation.ts` 的 `navigateTo` 改为 `router.navigate(path, { replace })`，删除了手动 `dispatchEvent(popstate)`
- [x] `App.tsx`：订阅 `router.subscribe` + 保留 popstate 监听；14 个页面改 `React.lazy` + 一个 `<Suspense>` 包裹
- [x] 新增 `.workspace-page-loading` 样式（`styles.css` 顶部）
- [x] 验证：**345 个用例全过（50 个文件）**，`npm run build` 成功，chunk 已按页面拆分

**第 1 步实测结果**

构建产物（`dist/assets/`，改前为单一 bundle）：

| chunk | 体积 | gzip |
| --- | --- | --- |
| index（框架 + 外壳，首屏） | 387.84 kB | 121.94 kB |
| useThreadTelemetry | 30.85 kB | 10.25 kB |
| TodayPage | 45.27 kB | 12.12 kB |
| PlanPage | 31.58 kB | 9.48 kB |
| ChatPage | 27.78 kB | 9.56 kB |
| GrowthPage | 22.47 kB | 7.48 kB |
| ResearchPage / ModelsPage / MemoryPage / TrajectoryPage | 各 11-13 kB | — |
| EvaluationPage / SkillsPage / GoalWorkspacePage / SchedulesPage / PlanningWorkspace / UsagePage | 各 3-10 kB | — |

**测试侧的改动**（懒加载让页面异步出现，原断言是同步的）：
- `App.test.tsx`：4 个用例改 `async` + `findBy*`，并引入 `const LAZY = { timeout: 8000 }`
- `AppRunIsolation.test.tsx`：2 处等待页面元素加长超时
- `NavigationRegression.test.tsx`：2 个用例改 `async` + `findBy*`

> 注意：全量并行跑时动态 import 会明显变慢（transform/import 阶段累计数十秒），所以等待懒加载页面的断言**必须**带较长超时，否则会出现"单跑通过、全量失败"的假失败。

**踩坑 1（编码）**：`App.tsx` 曾被 PowerShell 的 `WriteAllText` 与 `>` 重定向破坏编码（一次变 GBK 乱码、一次写成 UTF-16 LE）。**结论：改这个仓库的前端文件只能用 editor 工具或显式 `UTF8Encoding($false)` 写入，不要用 PowerShell 重定向。**

**踩坑 2（CSS `@import`，已在线上踩中）**：懒加载占位样式 `.workspace-page-loading` 曾被插在 `styles.css` **第一行**，而该文件开头是 `@import "./tokens.css"`。CSS 规范要求 `@import` 必须先于所有其他规则，于是 **tokens.css 被浏览器静默忽略**，整个应用布局崩溃（构建成功、测试全绿、只有真开页面才能发现）。构建产物体积是线索：CSS 从 163.27 kB 变 165.76 kB（多出的正是补回的 tokens）。已把该规则移到文件末尾修复。**教训：往含 `@import` 的 CSS 文件加规则，永远加在末尾；改完样式务必真开一次页面。**

### 4.2 第 2 步：TanStack Query 接管服务端状态 —— 部分完成（App 层，commit `fb8427a`）

- [x] 新建 `src/queries.ts`：集中管理 query keys（`bootstrap` / `threads`）与 `useBootstrapQuery` / `useThreadsQuery`
- [x] App 顶层 `getBootstrap` / `listThreads` 的 `useEffect + useState` 全部替换为 `useQuery`
- [x] 会话删除改为 `queryClient.setQueryData` 更新 threads 缓存；真人模式切换写回 bootstrap 缓存；新会话创建后 `invalidateQueries(["threads"])`
- [x] 新增 `__tests__/renderApp.tsx` helper：为 App 级测试提供独立的 QueryClient（`retry:false, gcTime:0`），5 个测试文件改用
- [x] 验证：50 文件 / 345 用例全过，构建成功
- [ ] **未做（后续）**：`setHumanMode` / `deleteThread` 换成 `useMutation`（当前通过 mutateAsync 风格保留在原 handler 里，缓存已归 Query 管）；页面级数据获取（ChatPage / TodayPage / PlanPage / ResearchPage 等）逐页迁移——每页一次提交

> 说明：App 层迁移后，服务端状态的"所有权"已归 Query 缓存，组件内不再保存副本。页面级迁移是机械性工作，但量大，建议按页拆提交。

### 4.4 组件收敛：审批卡片（commit `5266777`）—— ✅ 已完成

针对 front0919 代码评审发现的两个问题：

1. **`ChatToolApprovalCard` 单体化**（209 行 → 39 行）
   - 新建 `components/approval/toolDetails.tsx`：按 `tool_name` 注册渲染器（`create_plan_draft` / `modify_plan_document` / `activate_goal_plan` / `defer_action` / `record_action_feedback`），未注册工具回落通用文案
   - 卡片外壳只剩布局与按钮；**新增工具只需在注册表加一行**，不再往 if-else 链里塞分支
   - 抽出共享的 `stringParam` / `numberParam` / `FactList` 等，消除各渲染器间的重复
2. **激活预览重复渲染**（两份 → 一份）
   - 新建 `components/approval/ActivationPreview.tsx`：run 级 `ApprovalCard`（details prop）与对话内工具卡共用同一组件
   - `showProgramVersion` / `heading` props 处理两边的文案差异；用户可见文案零变化

验证：ChatToolApproval / GoalActivationApproval / App 三个测试文件全过，50 文件全量回归通过，构建通过。

> 踩坑记录：拆文件时用 editor 替换误删了 `type ToolRenderer` 声明，构建（tsc）报 TS2304。vitest 不做类型检查所以测试仍绿——**不能只看测试绿，必须跑 `npm run build` 做类型检查**。

### 4.3 第 3 步：Zustand 收敛流式状态 —— 未开始

- [ ] 新建 `stores/threadStreamStore.ts`
- [ ] `useThreadTelemetry` 改为薄封装（接口不变）
- [ ] 验证：ChatPage / 流式相关测试全绿；手动验证 SSE 流式、中断、恢复


---

## 5. 风险与注意事项

1. **WIP 冲突**：`front0919` 的 stash 里也改了 `api.ts`、`useThreadTelemetry.ts`、`ChatPage.tsx`。本分支完成后的合并方案：**先合入本分支，再 pop stash 手动解决冲突**（stash 里是功能新增，本分支是架构迁移，冲突点预计在 import 区和状态初始化区）。
2. **测试脆性**：部分测试依赖 `window.history` / popstate 行为（`NavigationRegression.test.tsx`、`AppRunIsolation.test.tsx`）。若 router 化导致失败，优先在测试里补 router 初始化，而不是回退实现。
3. **懒加载导致测试异步化**：组件测试若断言"立即出现"，懒加载后需改为 `await screen.findBy*`。预计影响少量 App 级测试。
4. **不要动 `design.md` 的约束**：保留 URL 恢复视图、幂等、CSRF、人工审批边界的现有行为。
5. **Node 版本**：见 §0.3，`EBADENGINE` 是警告，别被 PowerShell 的 exit code 误导。

---

## 6. 文件变更清单（随进度更新）

| 文件 | 变更 | 所属步骤 |
| --- | --- | --- |
| `frontend/package.json` / `package-lock.json` | 新增 3 个依赖 | 1 |
| `frontend/src/router.tsx` | 新增，路由配置 | 1 |
| `frontend/src/main.tsx` | Provider 装配 | 1 |
| `frontend/src/navigation.ts` | API 保留，内部 router 化 | 1 |
| `frontend/src/queries.ts` | 新增，query keys + App 层 hooks | 2 |
| `frontend/src/__tests__/renderApp.tsx` | 新增，App 测试的 QueryClient helper | 2 |
| `frontend/src/App.tsx` | 懒加载 + router 订阅 + Query 化 | 1、2 |
| `frontend/src/__tests__/App.test.tsx` 等多个测试 | `findBy*` + LAZY 超时 + renderApp | 1、2 |
| `frontend/src/components/approval/toolDetails.tsx` | 新增，按 tool_name 注册的审批渲染器 | 组件收敛 |
| `frontend/src/components/approval/ActivationPreview.tsx` | 新增，共享激活预览 | 组件收敛 |
| `frontend/src/components/ChatToolApprovalCard.tsx` | 209 行 → 39 行薄壳 | 组件收敛 |
| `frontend/src/components/ApprovalCard.tsx` | 复用 ActivationPreview | 组件收敛 |
| `frontend/src/index.css` 或 `styles.css` | 加载态样式 | 1 |


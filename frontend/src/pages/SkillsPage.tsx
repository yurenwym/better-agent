import { useEffect, useState } from "react";import { getSkills, listSkillVersions, listTrustedConnectors, setSkillVersionEnabled } from "../api";import type{SkillDefinition,SkillVersionRecord,TrustedConnectorRecord}from"../types";import AppToast from"../components/AppToast";
export default function SkillsPage({csrfToken}:{csrfToken:string}){const[skills,setSkills]=useState<SkillDefinition[]>([]),[connectors,setConnectors]=useState<TrustedConnectorRecord[]>([]),[selected,setSelected]=useState<SkillDefinition|null>(null),[versions,setVersions]=useState<SkillVersionRecord[]>([]),[error,setError]=useState(""),[notice,setNotice]=useState<{message:string;tone:"success"|"error"}|null>(null);useEffect(()=>{Promise.all([getSkills(),listTrustedConnectors()]).then(([s,c])=>{setSkills(s.skills);setConnectors(c.connectors)}).catch(e=>setError(e instanceof Error?e.message:"Skill 数据加载失败"));},[]);async function choose(skill:SkillDefinition){setSelected(skill);if(skill.skill_id){try{const result=await listSkillVersions(skill.skill_id);setVersions(result.versions);}catch{setVersions([])}}}async function toggle(v:SkillVersionRecord){try{const next=await setSkillVersionEnabled(v.version_id,v.status!=="ENABLED",csrfToken);setVersions(items=>items.map(x=>x.version_id===next.version_id?next:x));setNotice({message:next.status==="ENABLED"?"Skill 已启用":"Skill 已停用",tone:"success"});}catch(e){setNotice({message:e instanceof Error?e.message:"操作失败",tone:"error"});}}return <section className="control-page"><header className="control-hero"><div><span className="eyebrow">VERSIONED SKILLS</span><h2>Skill 平台</h2><p>声明式 ZIP、内容寻址版本、五层权限交集与可信 HTTPS Connector。</p></div><div className="control-hero-metrics"><strong>{skills.length}</strong><span>已安装</span><strong>{connectors.length}</strong><span>Connector</span></div></header>{error&&<div className="control-alert" role="alert">{error}</div>}<div className="control-master-detail"><section className="control-panel control-master"><div className="control-panel-head"><div><span className="eyebrow">INSTALLED</span><h3>已安装 Skill</h3></div></div>{!skills.length?<div className="control-empty compact"><h3>暂无 Skill</h3><p>安装 ZIP 后先预览权限，再确认授权。</p></div>:skills.map(s=><button className={`control-select-row${selected?.name===s.name?" active":""}`} key={s.name} onClick={()=>void choose(s)}><span><strong>{s.title}</strong><small>{s.description}</small></span><span className={s.enabled?"signal-good":"signal-warn"}>{s.enabled?"启用":"停用"}</span></button>)}</section><section className="control-panel control-detail">{!selected?<div className="control-empty"><h3>选择一个 Skill</h3><p>查看固定版本、工具授权、执行阶段和 Connector 引用。</p></div>:<><div className="control-panel-head"><div><span className="eyebrow">SKILL DETAIL</span><h3>{selected.title}</h3></div></div>{versions.map(v=><article className="skill-version-card" key={v.version_id}><div className="control-row-title"><strong>版本 {v.version}</strong><span className={`status-chip status-${v.status.toLowerCase()}`}>{v.status}</span></div><p>已授权工具 {v.granted_tools.length} / {v.requested_tools.length}</p><div className="chip-row">{v.requested_tools.length?v.requested_tools.map(t=><span className={v.granted_tools.includes(t)?"data-chip granted":"data-chip"} key={t}>{t}</span>):<span className="data-chip">Prompt-only</span>}</div><dl className="detail-list"><div><dt>执行阶段</dt><dd>{v.phases.join(" · ")||"无"}</dd></div><div><dt>Connector</dt><dd>{v.connectors.join(" · ")||"无"}</dd></div><div><dt>包摘要</dt><dd><code>{v.package_digest.slice(0,16)}</code></dd></div></dl><button className="button button-secondary" onClick={()=>void toggle(v)}>{v.status==="ENABLED"?"停用版本":"启用版本"}</button></article>)}</>}</section></div><section className="control-panel connector-panel"><div className="control-panel-head"><div><span className="eyebrow">TRUSTED HTTPS</span><h3>可信 Connector</h3></div><span className="control-count">{connectors.length}</span></div>{connectors.length?<div className="control-list">{connectors.map(c=><article className="control-row" key={c.version_id}><div><strong>{c.name} · v{c.version}</strong><p>{c.base_url}</p><div className="chip-row">{c.methods.map(m=><span className="data-chip" key={m}>{m}</span>)}</div></div><span className={c.verified_at?"signal-good":"signal-warn"}>{c.verified_at?"DNS 已验证":"待验证"}</span></article>)}</div>:<div className="control-empty compact"><h3>暂无 Connector</h3><p>Skill 不能直接声明任意 URL，只能引用这里维护的 HTTPS 连接器。</p></div>}</section>{notice&&<AppToast {...notice} onDismiss={()=>setNotice(null)}/>}</section>}

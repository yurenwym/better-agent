# M3-LIVE-20260909-016

015 在第一轮首错停止：researcher 未保留 S3 的原始执行时长 50，且 researcher 与 planner 的规范化文本相似度为 0.973865。015 共发出 7 次调用、耗时 21.376819 秒，随后精确清理 196 行并恢复 stable；它是失败证据，不作为 M3 阶段出口。

016 的修复范围：

- 删除会抵消分工的“三角色总合同”，每个 system prompt 只保留当前角色的专属输出合同。
- 明确角色合同高于当前 request；request 条件只能在当前角色范围内适用，不能扩大职责。
- researcher 必须保留冲突来源的原始主张和原始数字；planner 必须列出计算过程和依赖；critic 必须给出具体失败模式，禁止复述完整比较。
- 新增内容互补硬门禁：任意两个角色的规范化文本相似度必须低于 0.9；仅靠角色标签或少量措辞差异不能通过。
- 新增角色来源证据门禁：researcher 必须从 findings 引用 S3/S4，planner 必须引用 S1/S2，critic 必须引用 S3/S4；所有 source_refs 仍只能是 S1–S4。
- 失败诊断只导出当前批次 owner 的 turn、attempt、run 和 task，不读取或复制其他应用数据。

执行边界：

- 唯一当前 PostgreSQL 数据库 `better_agent`，不创建隔离数据库。
- 三轮冻结案例，每轮固定最多 7 次 DeepSeek `deepseek-v4-flash` 调用，总计最多 21 次。
- 单次最坏费用 `25,232 microusd`；批次费用硬上限 `529,872 microusd`（US$0.529872）。
- 最长 15 分钟；无 fallback；任一传输、模型、状态、互补性、来源、算术、质量、预算或清理错误立即停止。
- C 必须保留 `90/130` 两种可能，任何专家或最终答案出现 `150` 均失败。
- 批次结束后按 `m3-live-20260909-016-*` owner、`acceptance_batch=M3-LIVE-20260909-016` 和 manifest 关联精确删除测试数据并恢复执行前 stable。
- 不执行 Canary、候选发布、M4、M5、embedding 或其他后台付费任务。

016 必须经过无网络预检和完整相关回归后才可申请执行；源码摘要由预检现场生成。

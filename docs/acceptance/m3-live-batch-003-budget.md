# M3-LIVE-20260909-003 待批准预算

范围沿用002的m3-comparison-v1案例与m3-comparison-rubric-v1评分标准：三轮独立PostgreSQL数据库，真实对话入口、三个专家、协调器及同题单Agent基线；每轮最多7次，总计最多21次，无备用模型及额外重试额度，最长15分钟。模型deepseek-v4-flash，输入上限32,768、输出上限8,192。

价格依据2026-09-09已核验的DeepSeek官方峰值费率（输入cache miss US$0.44/1M、cache hit US$0.014/1M，输出US$1.32/1M），单次保守预留25,232 microusd，总硬上限US$0.529872。与002已发生两次请求的保守上限合计不超过US$0.580336；实际费用以usage/供应商账单为准。

改变：补充显式协作路由指令，以及失败轮次诊断导出。冻结指纹见m3-003-preflight.json。002批准不扩展到003；待用户确认后执行。异常即停止，不自动重启批次。原应用数据库不参与验收。

本批次完成调用后仍需对匿名最终回答逐项评分；不把AWAITING_QUALITY_REVIEW视作M3完成，不自动进入M4，不授权候选生成、embedding、Canary或发布。

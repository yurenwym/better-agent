# M3-LIVE-20260909-005 待批准预算

范围、模型、固定合成题及评分标准沿用004：三轮独立PostgreSQL，最多21次调用，每轮最多7次，最长15分钟，无备用模型、embedding或额外重试。任何失败立即停止，不自动重跑。

模型deepseek-v4-flash；固定输入32,768、输出8,192上限。按已核验峰值输入cache miss US$0.44/1M、cache hit US$0.014/1M及输出US$1.32/1M，单次预留25,232 microusd，本批次最高US$0.529872。002–004共六次请求的保守上限US$0.151392；含005最多US$0.681264。

本次只改变诊断：保存固定合成题产生的可见路由回复，先去除实际凭据并经过既有审计脱敏，再与finish_reason、协议诊断一起保存。不会保存真实用户正文、隐藏推理或凭据。不进一步放宽路由协议。冻结指纹见m3-005-preflight.json。

005执行后发现单Agent基线60秒单请求超时；本批次不重跑。后续006仅将单次客户端超时提高到120秒，其他边界不变，需按新指纹单独执行。

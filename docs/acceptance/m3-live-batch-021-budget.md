# M3-LIVE-20260909-021

020 第一轮的三个专家与 coordinator 均成功，角色最高相似度 0.384615，所有事实、来源和最终综合结论正确；单 Agent 基线未调用。旧 `budget_arithmetic_consistent` 正则跨句把“方案A 130分钟超预算。方案B 100分钟在预算内”错误拼接为“130分钟在预算内”，因此报告按当时门禁失败。020 使用 4 次调用并记账 `100,928 microusd`；清理 97 行后批次 run、invocation、bundle 均为 0，stable 恢复为执行前版本。

021 的修复与冻结证据：

- 预算矛盾检查限制在同一句/同分句，不能跨越句号、分号或换行关联其他方案。
- 新增正例：A=130>120 后另句 B=100 在预算内必须通过；原有反例“130分钟仍在两小时预算内”继续失败。
- 对 020 已保存产物离线回放：全部角色门禁通过；多 Agent 冻结质量五项均为 1，总分 5/5。该回放不追改 020 的失败记录。
- coordinator 后先检查角色和多 Agent 最终质量，成功后才调用单 Agent 基线。
- child 失败在 coordinator 前停止；显式专家协作由代码直接生成 V4 控制头。
- 每轮最多 5 次：三个专家、coordinator、同题单 Agent 基线；三轮总上限 15 次。
- “130分钟在/未超过两小时预算”、C=150、未知 source_ref、角色文本相似度 `>=0.8` 均硬失败。

执行边界：

- 唯一当前 PostgreSQL 数据库 `better_agent`，不创建隔离数据库。
- 三轮冻结案例，每轮最多 5 次 DeepSeek `deepseek-v4-flash` 调用，总计最多 15 次。
- 单次最坏费用 `25,232 microusd`；批次费用硬上限 `378,480 microusd`（US$0.378480）。
- 最长 15 分钟；无 fallback、无网络重试；任一传输、模型、状态、互补性、来源、算术、质量、预算或清理错误立即停止。
- 批次结束后按 `m3-live-20260909-021-*` owner、`acceptance_batch=M3-LIVE-20260909-021` 和 manifest 关联精确删除测试数据并恢复执行前 stable。
- 不执行 Canary、候选发布、M4、M5、embedding 或其他后台付费任务。

021 必须先通过无网络预检与完整相关回归，再单独获得明确批准。

执行前冻结证据：

- 无网络预检：通过。
- M3 完整相关回归：`285 passed`。
- 冻结源码摘要：`5ce75935f7af48395e3fa5c4d3a8d925676b4ed0ed84a437270b5e3300c7ade1`。
- 冻结案例摘要：`bc94ad19d9e4c57b1bc70668bb52dc00f466a292bf66ae01e381814693488254`。
- 用户于 2026-09-09 明确批准执行 `M3-LIVE-20260909-021`。

执行结果：`FAILED`，不得作为 M3 通过证据。

- 第 1 轮通过全部冻结质量门禁并完成同题单 Agent 基线。
- 第 2 轮三个专家及 coordinator 均成功，角色审计全部通过，最高角色相似度 `0.320359`；在同题基线调用前停止。
- 第 2 轮综合答案的算术、依赖、来源冲突和权限边界内容正确，但方案 A 段未显式写出 `S1`。冻结门禁因此同时判定 `a_budget`、`source_support` 失败。
- 共执行 9 次模型调用，记账 `227,088 microusd`（US$0.227088），未超过批次硬上限。
- 当前数据库精确删除 259 行；批次 threads、agent runs、model invocations、runtime bundles、cost budgets 均为 0。
- stable 恢复为执行前的 `bundle_8040b4cc18e6bf2282d15992`。
- 原始报告保留在 `m3-live-results-2026-09-09-021.json`，失败轮产物保留在 `m3-live-20260909-021-round-2/round-diagnostics.json`；不追写为通过。

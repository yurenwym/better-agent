# M3-LIVE-20260909-014

013 的运行器结果为 PASSED，但事后角色审计发现三个专家输出未形成稳定互补，并出现将 C 的 `20+90+20` 错算为 150 的错误，因此 013 不作为阶段出口。014 增加独立角色系统合同、角色输出互补/来源/算术硬门禁，以及最终答案 C=90/130 且不得出现 150 的冻结断言。

范围：唯一当前 PostgreSQL 数据库 `better_agent`，批次结束后按 `m3-live-*` owner 与 `acceptance_batch` 精确删除测试数据并恢复 stable。三轮，每轮最多 7 次、总计最多 21 次 DeepSeek `deepseek-v4-flash` 调用；无 fallback；最长 15 分钟；硬上限 `529,872 microusd`（US$0.529872）；任一模型、状态、角色互补、事实、质量或清理失败立即停止。

# M2-LIVE-20260909-001

目标：验收 M2 对话与上下文，不进入 M3，不启用候选发布或 Canary。

固定流程连续执行三轮：结构化 ask、回答续接、运行中 stable 切换、仅更新对话目标且不修改计划、明确计划修订。检查 ask 续接保持原 bundle 和根预算，新独立任务使用新根，目标更新不静默修改计划，明确修订生成新版本，所有模型调用记录实际 bundle、prompt/request digest 与共享根预算。

边界：只使用当前 PostgreSQL 数据库 `better_agent`；每轮在临时 schema 中执行，批次结束后删除 schema，不保留测试数据；不创建第二数据库。最多每轮10次、总计30次 DeepSeek `deepseek-v4-flash` 请求；不使用备用模型；最长20分钟；首个失败停止。按每次最坏 `25,232 microusd` 计，最坏总费用 `756,960 microusd`（US$0.756960）。

执行前门禁：后端离线回归、前端回归与构建、当前库 Alembic head、当前库回滚探针均通过。只有三轮连续通过且临时 schema 删除成功，M2 才可标记完成。

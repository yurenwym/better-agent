# v2 真实模型分阶段验收

2026-09-10，用户授权由代理选择策略和费用上限。本次选择官方 `deepseek-flash`，总上限 US$1：计划编译阶段 US$0.10、Prompt 受控诊断 US$0.90。两个阶段使用独立测试 PostgreSQL 容器；无备用模型、无网络重试，未修改生产数据库或发布生产候选。

## 结果

| 阶段 | 真实调用 | 高峰价估算费用 | 结果 |
| --- | ---: | ---: | --- |
| Memory→真实计划编译 | 2 | US$0.004400 | PASS |
| 12 场景双臂写作及盲评 | 36 | US$0.006207 | 执行完成；候选质量门禁 FAIL |
| 合计 | 38 | **US$0.010607** | 未超过 US$1 上限 |

费用根据实际 usage 与冻结高峰价计算，不是供应商最终账单。2026-09-10 读取的官方价格为每百万 token：cache miss US$0.30、cache hit US$0.006、output US$1.20。来源：https://api-docs.deepseek.com/quick_start/pricing 。原始页面留存在 `backend/artifacts/learning-v2-official-pricing.html`。旧配置名 `deepseek-v4-flash` 已由官方映射至新 Flash，本次显式使用 `deepseek-flash`。

## Memory 的真实消费者证据

用户约束“以后每次练习最多30分钟”自动形成 revision。新线程真实模型编译生成 15、30、15 分钟三个行动，每项不超过30分钟，总计60分钟，保留本次每日60分钟要求。PostgreSQL 模型调用快照包含 bundle 和该 revision。两次 invocation 均保留调用状态、预算和费用证据。

输出：`learning-v2-live-memory-2026-09-10.json`；执行日志：`backend/artifacts/learning-v2-live-memory.log`。测试 1 passed，约23秒。

## Prompt 配对诊断

在调用前保存12个人工场景和固定候选片段，不修改留出后重跑。覆盖因果、缺少分母、来源冲突、时间外推、阈值、来源中的无关指令。每个场景基线/候选使用同一模型、温度0和生产写作提示词构造器及 section 后处理，裁判按预定奇偶交换左右，无法从左右标签直接识别候选。

- 候选4胜、6平、2负。
- 候选基础确定性约束12/12；裁判安全12/12。
- 冻结门禁要求无败例，因此 **FAIL**。pytest 的通过仅表示实验执行和存档完成，不能解释为候选质量通过。
- 两个败例都位于来源冲突场景。一个裁判理由是引用支撑更精确，另一个明确承认仅引用标识格式差异、不代表事实改善。因此不能把该裁判排名当作已经证明的行为回归或提升。
- 裁判与写作模型相同，不是独立模型，也没有人工校准。

追加离线检查发现严格完整 source ID 检查会拒绝 `0_a` 之类缩写，但生产 `render_citations` 已支持唯一 `source_` 别名。使用已保存输出和冻结来源回放真实引用渲染器，**24/24 可解析**。因此没有为这个现象修改生产代码，也没有将旧 FAIL 改写为 PASS。

输出：`learning-v2-live-prompt-2026-09-10.json`；调用前计划：同名 `.plan.json`；执行日志：`backend/artifacts/learning-v2-live-prompt.log`。测试1 passed，约68秒。

## 证据边界与下一步

真实 Memory 消费已验证；真实 Prompt 受控诊断已完成并发现候选未达到门禁。此轮不是60个独立生产来源的发布评测，也没有验证完整生产研究抓取、长期 Skill 效果或生产 canary。

不继续盲目扩大样本或重跑同套输入，不花费剩余预算追求通过。现有12个人工场景以及历史合成场景不应伪装成60个独立生产样本。后续正式 Prompt 发布评测需要积累并冻结合格原始任务来源，校准最终交付评分，然后执行完整自动学习 cycle 和后续任务效果验证。

当前结论：**真实计划链路通过；Prompt 候选不发布；v2 全部真实效果验收仍未通过。**

后续用户授权的12任务模拟规划/记忆闭环已完成，见 [模拟任务验收](learning-v2-simulated-journeys-2026-09-10.md)：12/12通过，追加39次真实调用，US$0.095058；累计US$0.105665。该结果不覆盖本报告的 Prompt FAIL。

# M2-LIVE-20260909-005

004 已验证 ask 续接、计划创建、运行版本与根预算绑定，以及“只更新对话约束不改计划”的边界。最后的明确计划修订在发起第 11 次网络请求前被每轮 10 次硬上限阻止；失败报告保留在 `m2-live-results-2026-09-09-004.json`，临时 schema 已删除。

005 不提高调用上限，也不缩减“对话 -> 询问 -> 计划修订”场景。根据 M2 的“显式动作走确定路径，歧义才分类”要求，无研究信号的普通请求不再调用研究分类器；明确要求或明确禁止计划文档写入时不再调用模型做重复意图分类。

边界：只使用当前 PostgreSQL 数据库 `better_agent`；每轮在临时 schema 中执行，结束后删除 schema，不创建第二数据库。连续三轮，每轮最多 10 次、总计最多 30 次 DeepSeek `deepseek-v4-flash` 请求；无备用模型；最长 20 分钟；首个失败停止。005 最坏费用 `756,960 microusd`（US$0.756960）；连同此前 22 次请求按逐次最坏值保守累计为 `1,312,064 microusd`（US$1.312064）。

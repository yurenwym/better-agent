# M2-LIVE-20260909-008

007 已连续三轮通过，但随后的完整 M2 回归发现计划修复器会把泛化标题 `# Answer` 误判为可保存计划。该问题已收紧：仅明确含计划、规划、安排、方案或 `plan`/`schedule` 的一级标题可在已确认文档意图路径中规范化；其他内容继续硬失败。由于生产代码在 007 后变化，007 只保留为成功历史，不作为最终代码版本的阶段出口。

008 基于收紧后的最终代码重新开始连续三轮。完整 M2 后端回归为 `345 passed, 3 skipped`；前端为 `203 passed`，生产构建成功。

边界：只使用当前 PostgreSQL 数据库 `better_agent`，不创建第二数据库。连续三轮，每轮最多 10 次、总计最多 30 次 DeepSeek `deepseek-v4-flash` 请求；无备用模型；最长 20 分钟；首个失败停止。008 最坏费用 `756,960 microusd`（US$0.756960）；连同此前 50 次请求按逐次最坏值保守累计为 `2,018,560 microusd`（US$2.018560）。

收尾更正（2026-09-09）：008 报告中的 `temporary_schema` 和 `cleanup.temporary_schema_removed` 是运行器旧字段，不能证明存储隔离。事后核验确认 001–008 实际写入 `public`；这些行已按 `m2-live-*` owner 和 `acceptance_batch` manifest 精确删除，累计删除 4,752 行，stable 已恢复为 `bundle_8040b4cc18e6bf2282d15992`。数据库全字段扫描未再发现 `m2-live-`，普通 7 个线程未受影响。运行器现已改为明确声明当前数据库并在每批结束时执行批次级清理。

# M3 验收缺口审计（2026-09-09）

当前结论：M3 未完成。旧预算单及四调用驱动不能单独证明开发方案第 6 节的全部出口；在补齐案例和证据前，不申请执行该旧批次。

## 后续进展：002 草案

驱动现已走持久化计划材料→真实对话入口→专家任务→单 Agent 基线。每轮含辅助判断共7次，三轮上限21次；价格核验和暂定预算见 `m3-live-batch-002-draft.md`。匿名最终回答评审包及固定五项评分标准已实现，真实质量评分仍未执行。

协调器汇总和安全评判复用带心跳的取消等待；新建协调器允许三次领取（分派、汇总及一次恢复）。隔离 PostgreSQL 测试覆盖计划版本冻结、上下文隔离、完整对比驱动、服务重建、取消及租约接管。旧的阶段缺口列表保留为审计历史，不能据此忽略这些后续证据；研究来源追溯、全部质量出口和真实三轮仍需完成核对。

本次完整后端回归：862 passed、3 skipped、1 failed，耗时430秒。唯一失败为 embedding 默认缺钥测试继承外部 credential selector，已在该测试中显式清理 selector；随后整个 embedding 模块9 passed。未更改生产 embedding 逻辑，修复后未重跑全量。git diff --check 通过。未执行真实 M3 模型批次。

## 已修复并验证

- 真实请求失败时，驱动根据 attempt 记录输出 network_started；失败前未调用与已尝试调用分别有回归覆盖。
- 部分交付检查产物的 incomplete、failed_roles 和成功专家结果，不能仅凭根任务 SUCCEEDED 通过。
- 对话交接通过 parent_turn_id 从同用户、同会话的持久化 turn 继承 runtime_bundle_id，避免重新读取 stable 或重新分配 Canary。
- 快照保留当前请求、消息 ID，以及已经按 token 预算装配的完整历史；移除末尾 12 条消息截断。
- 专家系统提示明确 researcher/planner/critic 职责，保留原始约束优先和来源不可编造的要求。沿用 expert_system_prompt，因此运行与既有提示词 digest 渲染共享定义。

上述检查是离线证据，不证明真实模型遵从角色分工，也不证明多专家有质量收益。

## 真实批次前必须完成

1. 将实际 conversation → expert 入口加入隔离 PostgreSQL 验收，使用持久化会话、计划/研究来源及版本；当前驱动直接 create_run 的测试标签不能替代自动上下文装配。
2. 冻结同题单 Agent 与多 Agent 案例、盲评规则、质量及费用指标；同时保留负面例。当前四调用路径没有单 Agent 基线。
3. 核对取消中的外部调用终止、租约接管、重启恢复；不能以取消后的手工 complete 拒绝替代全部故障场景。
4. 保存各角色请求/prompt/context digest、产物、来源和版本证据，并检查角色互补、矛盾与未决事项。
5. 重新计算完整批次的调用次数、超时与最坏费用，冻结代码/案例/提示词版本，再提交具体付费范围。旧驱动每轮 4 次硬限制、三轮总 18 次的预算未覆盖完整对比设计，不视为新范围授权。
6. 获批后完成三轮连续真实验收，逐项审计证据，才进入 M4。M4/M5 占位入口不表示阶段完成。

## 本次验证记录

- 专家角色、提示词策略、worker 与 Canary 分配相关回归：21 passed。
- 新增 `backend/tests/integration/test_m3_conversation_handoff.py`：隔离 PostgreSQL 检查通过（1 passed）。经过真实会话提交/worker/专家任务服务，检查持久化历史进入三个专家快照、其他会话材料排除、当前请求与来源 ID 保留、stable 切换不改变原 turn 版本。模型显式模拟，因此不计入三轮真实模型验收；计划/研究来源版本和单/多 Agent 对比仍待补齐。
- 前序对话/Agent/驱动回归：71 passed；随后补充的交接上下文针对性检查通过。
- 尚需在最终驱动冻结后执行完整回归与 PostgreSQL 真实批次。

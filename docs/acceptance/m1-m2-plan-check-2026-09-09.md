# 按可靠性开发方案复核 M1 / M2

依据：docs/architecture/reliability-development-plan-2026-09-08.md，第4、5、9节及Q12/Q14。2026-09-09用户要求先检查已开发的M1/M2，再依方案顺序继续。

## 结论

- M1：存在历史真实通过报告m1-live-results-2026-09-08-005.json，顶层PASSED，三轮独立数据库记录、混合检索、Episode归因、受限回答及副作用检查可查。保留该阶段历史完成结论；不将旧结果表述为所有后续代码变动均重新经过真实验证。
- M2：已有对话、计划上下文、询问、目标调整、路由与费用实现和测试，但仓库未找到独立M2真实验收批次报告。m1-memory-development.md仍写“真实阶段验收待分别授权”。依据方案要求，目前不能确认M2已经完成阶段交付。
- M3：此前真实002/003/004均在第一轮失败；005尚未获批执行。现按顺序先核对并补齐M2，不继续试跑M3。

## 本次实际修复

M2第4项要求运行中任务不能被动态stable策略覆盖，但startup.py中的共享prompt_policy回调一直读取当前stable。现改为RoutedModelGateway.prompt_policy：优先使用当前ModelCallContext的runtime_bundle_id，只有没有绑定版本的新调用读取stable；指定版本不存在时明确失败。

对话模型、执行模型、研究模型及计划编译器复用这一回调，不新增存储。回归验证stable切换后四个消费者仍读取旧任务版本，调用上下文退出后新调用读取新stable，丢失固定版本不静默回退。

## 验证及剩余出口

- 记忆、归档、上下文、计划上下文、目标调整、询问、提示词和预算路由检查：118 passed。
- 本次修复的提示词绑定、路由和启动回归：24 passed。
- git diff --check通过；本轮没有启动真实模型、embedding或后台服务。
- 仍须逐项核对M2的版本化目标/约束来源、依赖上下文不可静默裁剪、永久模型错误分类、跨辅助/专家/修复调用的根任务共享预算，以及对话→询问→计划修订三轮真实出口。现有测试通过不替代这些完整要求。
- M2正式出口满足后，按M3→M4→M5推进。已有工作树改动与失败报告均保留。

本次生产文件修改使之前M3-005预检源码指纹过期；之后如执行须先重新冻结，不使用过期记录发起调用。

## M2 后续：永久错误分类与传播

HTTP 402现独立归类payment，提供不含供应商原始响应的付款/余额提示。OpenAI-compatible、Anthropic、Gemini共用HTTP状态分类，测试验证401/402均只发一次请求，不因默认重试额度多次调用。

计划、研究、记忆意图分类和专用询问生成不再把payment/authentication/configuration/budget/cancelled吞为普通否定结果；向上返回错误，保留非永久结构错误的既有降级。相关网关、分类和路由回归89 passed。

这完成了M2第5项的一部分。研究引擎等其他消费者的异常传播、上下文完整性与根任务预算仍需继续审计；尚未执行M2真实三轮。

# M3-LIVE-20260909-004 待批准预算

沿用003范围、模型、案例m3-comparison-v1和评分标准m3-comparison-rubric-v1。三轮独立PostgreSQL，最多21次模型调用，每轮最多7次，最长15分钟，无备用模型、embedding或额外重试。

deepseek-v4-flash：固定输入上限32,768、输出上限8,192。按2026-09-09已核验的峰值输入US$0.44/1M、缓存命中US$0.014/1M、输出US$1.32/1M，每次保守预留25,232 microusd，批次硬上限US$0.529872。002与003各两次调用合计保守上限US$0.100928；加上本批次上限累计不超过US$0.630800。

改变：规范化合法的完整JSON控制头（包括多行或缺末尾换行），然后执行原有严格校验；记录原始协议诊断元数据。冻结代码/案例/评分标准见m3-004-preflight.json。任何失败即停，不自动重跑；此前批准不扩展到004。

本批次仅采集M3真实对话协作与同题基线证据，之后仍需质量评审和阶段出口核对，不自动进入M4，不授权Canary或发布。

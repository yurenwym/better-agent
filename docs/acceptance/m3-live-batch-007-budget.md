# M3-LIVE-20260909-007 待批准预算

这是006空基线响应后的最小调整批次：三轮独立PostgreSQL、m3-comparison-v1、m3-comparison-rubric-v1，每轮最多7次、总计最多21次、最长15分钟、无备用模型。单Agent基线请求输出上限降为4096以降低空响应风险；其他角色仍为8192，费用预留仍按固定profile最坏8192输出计算。

批次最坏费用仍为US$0.529872；失败立即停止，不自动进入M4。执行前重新核验冻结指纹和价格快照。

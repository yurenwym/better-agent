# M3-LIVE-20260909-017

016 在第一轮首错停止：三个专家均把 130 分钟错误描述为“两小时预算内”，planner 因缺少正确的 120 分钟比较触发 `planner_focus`。回放加强后的门禁还会同时拒绝该批次的 `budget_arithmetic_consistent=false` 和 researcher/planner 文本相似度 0.843312。016 共 7 次成功且已计价调用，实际记账 `176,624 microusd`；清理 196 行后批次 owner、invocation、bundle 均为 0，stable 恢复为执行前版本。

017 的修复与新增证据：

- 冻结任务显式写明“两小时（120分钟）”，避免单位换算被模型隐含遗漏。
- planner 合同要求先统一时间单位再计算；critic 合同要求复核单位换算和大小比较。
- 所有专家增加预算算术硬门禁：任何“130分钟在/未超过两小时预算”的表述均失败。
- 角色文本最高相似度门槛从 `<0.9` 收紧为 `<0.8`；016 的 0.843312 将被拒绝。
- researcher 只审计需交叉核验、相互冲突或证据不足的来源，无需复述没有来源争议的完整方案。
- 顶层成功或失败报告在清理前保存本批次实际 attempt 数、计价状态和 `charged_microusd`；网络摘要不再被误当作费用证据。

执行边界：

- 唯一当前 PostgreSQL 数据库 `better_agent`，不创建隔离数据库。
- 三轮冻结案例，每轮最多 7 次 DeepSeek `deepseek-v4-flash` 调用，总计最多 21 次。
- 单次最坏费用 `25,232 microusd`；批次费用硬上限 `529,872 microusd`（US$0.529872）。
- 最长 15 分钟；无 fallback；任一传输、模型、状态、互补性、来源、算术、质量、预算或清理错误立即停止。
- C 必须保留 `90/130` 两种可能，任何专家或最终答案出现 `150` 均失败。
- 批次结束后按 `m3-live-20260909-017-*` owner、`acceptance_batch=M3-LIVE-20260909-017` 和 manifest 关联精确删除测试数据并恢复执行前 stable。
- 不执行 Canary、候选发布、M4、M5、embedding 或其他后台付费任务。

执行前结果：无网络预检通过，源码摘要 `ad72b7c209c8a4e033a548f09959a3b9a91609789528be48f86568e657c1f1f6`，冻结案例摘要 `bc94ad19d9e4c57b1bc70668bb52dc00f466a292bf66ae01e381814693488254`；完整 M3 相关回归 `282 passed`。017 仍需单独获得明确批准。

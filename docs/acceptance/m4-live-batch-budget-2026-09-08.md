# M4 真实研究验收预算单

批次：`M4-LIVE-20260909-003`

状态：用户已明确批准，`M4-LIVE-20260909-003` 已执行并通过；正式结论见 `m4-acceptance-final-2026-09-09.md`。本预算单不授权任何新批次或 M5。

前两批次均在第一轮首错停止且未自动重跑：`001` 因引用别名/稳定通道验收驱动缺陷发生 8 次调用，US$0.201856；`002` 因结构化截断输出被错误接受而产生 PARTIAL，发生 10 次调用，US$0.252320。两批测试数据均已完全清理。若批准本批次，三批累计最坏支出为 US$1.211136。

## 范围

在当前唯一 PostgreSQL 数据库 `better_agent` 中运行三个冻结官方资料主题，每个主题一轮：Python asyncio、PostgreSQL EXPLAIN、Git 撤销操作。每轮只允许两个已冻结官方 URL；逐个验证 DNS/SSRF、重定向后的官方域名、正文哈希和检索时间。

每轮必须得到 `COMPLETED`，两个明确要求都必须在“要求→证据→结论→引用”矩阵中标为 supported；引用来源、证据 ID、原文字符位置、原文哈希及来源版本必须一致。任何来源不可用、引用错配、缺失要求、永久模型错误、预算错误或清理错误都会让批次立即失败。

## 硬上限

- 模型：`deepseek-v4-flash`，无 fallback model。
- 每轮最多 10 次模型网络调用，三轮最多 30 次。
- 结构化输出修复重试 0 次；provider 网络重试 0 次。
- 官方来源 HTTP 请求最多 12 次；不调用付费搜索服务。
- 最长 30 分钟；首个失败立即停止，不自动重跑。
- 每次调用最坏费用 `25,232 microusd`，沿用已在 `M3-LIVE-20260909-022` 冻结并实际计费验证的价格快照费率。
- 批次最坏费用 `756,960 microusd`，即 US$0.756960。
- 每轮 ROOT/DAILY 上限 `252,320 microusd`；每次 INVOCATION 上限 `25,232 microusd`；批次驱动另设总调用和总费用硬门禁。

## 数据与清理

不创建隔离数据库。所有 owner 使用 `m4-live-20260909-003-%` 前缀，所有临时 runtime bundle 带 `acceptance_batch=M4-LIVE-20260909-003`。worker 只精确领取本批次指定 job，不会消费当前库的其他排队任务。构建验收 runtime 时禁止自动切换 stable channel。

无论成功或失败，脚本都会按 owner/bundle 标识删除本批次 research、thread、turn、模型调用、价格、预算和 bundle 数据，并验证 stable bundle 未改变。不会删除或改写应用既有数据。

## 执行门禁

默认命令只生成 `NOT_AUTHORISED` 预检，不连接数据库、不访问官方来源、不调用模型。只有用户明确批准上述批次、来源、调用上限和 US$0.756960 最坏费用后，才能设置 `M4_LIVE_APPROVED=1` 并配合 `--execute` 执行一次。

# M2-LIVE-20260909-006

005 在 ask 续接的强制计划文档阶段收到约 4 KB 正文，但供应商输出没有通过严格的首行 artifact 自述校验。四次网络请求均成功；批次在第一轮立即停止，失败报告保留在 `m2-live-results-2026-09-09-005.json`，临时 schema 已删除。

006 只修复已经由持久化 ask 关系或显式文档动作确认的路径：供应商若返回带一级 Markdown 标题的完整文档正文，应用生成规范 v2 `plan_document` 控制头；未确认的普通回答不会被强制保存。验收脚本也会在 schema 删除前记录 Turn Job 的内部错误摘要。

边界：只使用当前 PostgreSQL 数据库 `better_agent`；每轮在临时 schema 中执行，结束后删除 schema，不创建第二数据库。连续三轮，每轮最多 10 次、总计最多 30 次 DeepSeek `deepseek-v4-flash` 请求；无备用模型；最长 20 分钟；首个失败停止。006 最坏费用 `756,960 microusd`（US$0.756960）；连同此前 26 次请求按逐次最坏值保守累计为 `1,412,992 microusd`（US$1.412992）。

# Reflection

Use after a run reaches an outcome or completion. Summarize observations, compare execution with the approved plan, and propose preference or habit candidates with evidence event IDs. Allowed tools are `local_time`, `calculator`, and `read_note`; candidates require user confirmation before affecting future context.


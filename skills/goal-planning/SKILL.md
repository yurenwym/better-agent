# Goal planning

Use when a new goal lacks an approved executable plan. Return a short structured plan with ordered atomic steps, dependencies, and a clarification question when the goal is underspecified. Allowed tools are `local_time`, `calculator`, and `read_note`; never write files or change runtime state directly.

